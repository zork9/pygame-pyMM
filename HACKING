- grep for FIX
- z,x buttons (talk & fight with sword)
- arrows movement


git FAQ :

if contributing, use the following 2 commands

git clone httpss://github.com/zork9/pygame-pyMM
git remote set-url origin http://github.com/zork9/pygame-pyMM.git

Then commit and maybe set you user.name and user.email with git --config
